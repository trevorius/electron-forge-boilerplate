import ChatInterface from "@/components/chat/ChatInterface"

export default function Layout() {
  return (
    <div className="fixed inset-0 bg-gradient-to-br from-slate-900 to-slate-700 text-white">
      <ChatInterface />
    </div>
  )
}
